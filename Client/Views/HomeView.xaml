<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Client.Views.HomeView"
             Title="Главная"
             Shell.NavBarIsVisible="True"
             Background="#111"
              xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
            xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome">
    <ScrollView>
        
  
    <Grid BackgroundColor="#80373737"  RowDefinitions="auto,1*">
           
            <StackLayout Grid.Row="0" Margin="10">
               
                <Label FontSize="20" TextColor="White" Margin="0,0,0,5" Text="Приветствуем в 'название магазина'"></Label>
            <Button Clicked="Button_Clicked" Text="Авторизация" FontSize="20" CornerRadius="5" Background="#373737"></Button>
        </StackLayout>

        <Border Padding="10"  Background="black" Stroke="transparent" StrokeThickness="0" Grid.Row="1">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10,10,0,0"></RoundRectangle>
            </Border.StrokeShape>
            <StackLayout>
                <material:TextField Margin="0,10,0,0" BorderColor="White" Icon="{FontImageSource Color=White, FontFamily=FASolid, Glyph={x:Static fa:Solid.MagnifyingGlass}}" AccentColor="White"   TextColor="White" Title="Поиск по названию" TitleColor="White">
                    
                </material:TextField>
                <ScrollView VerticalScrollBarVisibility="Never" HorizontalScrollBarVisibility="Always">
                    <HorizontalStackLayout>
                        <Grid Margin="10" HeightRequest="150" WidthRequest="300" Background="#80373737">
                            <Label Text="Пример акции" HorizontalOptions="Center" VerticalOptions="Center" FontSize="20" TextColor="White"></Label>
                        </Grid>
                        <Grid Margin="10" HeightRequest="150" WidthRequest="300" Background="#80373737">
                            <Label Text="Пример акции" HorizontalOptions="Center" VerticalOptions="Center" FontSize="20" TextColor="White"></Label>
                        </Grid>
                        <Grid Margin="10" HeightRequest="150" WidthRequest="300" Background="#80373737">
                            <Label Text="Пример акции" HorizontalOptions="Center" VerticalOptions="Center" FontSize="20" TextColor="White"></Label>
                        </Grid>
                        <Grid Margin="10" HeightRequest="150" WidthRequest="300" Background="#80373737">
                            <Label Text="Пример акции" HorizontalOptions="Center" VerticalOptions="Center" FontSize="20" TextColor="White"></Label>
                        </Grid>
                    </HorizontalStackLayout>
                </ScrollView>
            
                    <Label FontSize="20" TextColor="White" Text="Популярные Категории" Margin="5" FontAttributes="Bold">
                    
                </Label>
                    <HorizontalStackLayout>
                        <Grid Margin="10" Padding="10" HeightRequest="300" WidthRequest="220" Background="#80373737">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.8*"></RowDefinition>
                                <RowDefinition Height="0.2*"></RowDefinition>
                                <RowDefinition Height="0.2*"></RowDefinition>
                                <RowDefinition Height="0.3*"></RowDefinition>
                                <RowDefinition Height="0.2*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Image Source="withoutbg.png"></Image>
                            <Label Grid.Row="1" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>
                            <StackLayout Grid.Row="2" Orientation="Horizontal">
                                <Image HeightRequest="15" VerticalOptions="Center" Source="{FontImageSource Color=Gray,Glyph={x:Static fa:Solid.Star}, FontFamily=FASolid}" ></Image>
                                <Label Margin="5,0,0,0"  VerticalOptions="Center" FontAttributes="Bold" TextColor="Gray" FontSize="15">Нет отзывов</Label>
                            </StackLayout>
                            <Label Grid.Row="3" TextColor="White" FontSize="18">Морозильная камера Haier HF-82WAA</Label>

                        </Grid>
                      
                    </HorizontalStackLayout>

                    <material:TabView>
                        <material:TabView.TabHeaderItemTemplate>
                            <DataTemplate>
                                <Button
                                Text="{Binding Title}" 
                                Command="{Binding Command}"
                                 Margin="10,0,0,0"
                                  FontAttributes="Bold"
                                    Padding="6"
                                    CharacterSpacing="1"
                                    
                                >
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BackgroundColor" Value="White" />
                                            <Setter Property="TextColor" Value="Black" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Button" Binding="{Binding IsSelected}" Value="False">
                                            <Setter Property="BackgroundColor" Value="#80373737" />
                                            <Setter Property="TextColor" Value="White" />
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                            </DataTemplate>
                        </material:TabView.TabHeaderItemTemplate>
                        <material:TabItem Title="Новинки">
                           
                            <HorizontalStackLayout>
                                <Grid Margin="10" Padding="10" HeightRequest="250" WidthRequest="220" Background="#80373737">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="0.8*"></RowDefinition>
                                        <RowDefinition Height="0.2*"></RowDefinition>
                                        <RowDefinition Height="0.2*"></RowDefinition>
                                        <RowDefinition Height="0.3*"></RowDefinition>
                                        <RowDefinition Height="0.2*"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Image Source="withoutbg.png"></Image>
                                    <Label Grid.Row="1" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>
                                    <StackLayout Grid.Row="2" Orientation="Horizontal">
                                        <Image HeightRequest="15" VerticalOptions="Center" Source="{FontImageSource Color=Gray,Glyph={x:Static fa:Solid.Star}, FontFamily=FASolid}" ></Image>
                                        <Label Margin="5,0,0,0"  VerticalOptions="Center" FontAttributes="Bold" TextColor="Gray" FontSize="15">Нет отзывов</Label>
                                    </StackLayout>
                                    <Label Grid.Row="3" TextColor="White" FontSize="18">Морозильная камера Haier HF-82WAA</Label>

                                </Grid>

                            </HorizontalStackLayout>
                        </material:TabItem>
                        <material:TabItem Title="В тренде" >
                            <HorizontalStackLayout>
                                <Grid Margin="10" Padding="10" HeightRequest="250" WidthRequest="220" Background="#80373737">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="0.8*"></RowDefinition>
                                        <RowDefinition Height="0.2*"></RowDefinition>
                                        <RowDefinition Height="0.2*"></RowDefinition>
                                        <RowDefinition Height="0.3*"></RowDefinition>
                                        <RowDefinition Height="0.2*"></RowDefinition>
                                    </Grid.RowDefinitions>
                                    <Image Source="withoutbg.png"></Image>
                                    <Label Grid.Row="1" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>
                                    <StackLayout Grid.Row="2" Orientation="Horizontal">
                                        <Image HeightRequest="15" VerticalOptions="Center" Source="{FontImageSource Color=Gray,Glyph={x:Static fa:Solid.Star}, FontFamily=FASolid}" ></Image>
                                        <Label Margin="5,0,0,0"  VerticalOptions="Center" FontAttributes="Bold" TextColor="Gray" FontSize="15">Нет отзывов</Label>
                                    </StackLayout>
                                    <Label Grid.Row="3" TextColor="White" FontSize="18">Морозильная камера Haier HF-82WAA</Label>

                                </Grid>

                            </HorizontalStackLayout>
                        </material:TabItem>
                    </material:TabView>
                    
                    <Label FontSize="20" TextColor="White" Text="Вы смотрели" Margin="5" FontAttributes="Bold">

                    </Label>
                  
           
                    <ScrollView>
                        
                    <FlexLayout Direction="Column" Wrap="Wrap" JustifyContent="SpaceAround" HeightRequest="240">
                        <Grid Margin="5"  Padding="0" HeightRequest="100" WidthRequest="220" Background="#80373737">
                            <Grid.RowDefinitions>
                            
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.7*"></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Image  Source="withoutbg.png"></Image>

                            <StackLayout Grid.Column="1" VerticalOptions="Center">
                                <Label Grid.Column="1" Grid.Row="0" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>

                                <Label Grid.Column="1" Grid.Row="1" TextColor="White" FontSize="15">Морозильная камера Haier HF-82WAA</Label>
                            </StackLayout>
                          

                        </Grid>
                            <Grid  Margin="5" Padding="0" HeightRequest="100" WidthRequest="220" Background="#80373737">
                            <Grid.RowDefinitions>

                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.7*"></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Image  Source="withoutbg.png"></Image>

                            <StackLayout Grid.Column="1" VerticalOptions="Center">
                                <Label Grid.Column="1" Grid.Row="0" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>

                                <Label Grid.Column="1" Grid.Row="1" TextColor="White" FontSize="15">Морозильная камера Haier HF-82WAA</Label>
                            </StackLayout>


                        </Grid>
                            <Grid Margin="5"  Padding="0" HeightRequest="100" WidthRequest="220" Background="#80373737">
                            <Grid.RowDefinitions>

                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.7*"></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Image  Source="withoutbg.png"></Image>

                            <StackLayout Grid.Column="1" VerticalOptions="Center">
                                <Label Grid.Column="1" Grid.Row="0" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>

                                <Label Grid.Column="1" Grid.Row="1" TextColor="White" FontSize="15">Морозильная камера Haier HF-82WAA</Label>
                            </StackLayout>


                        </Grid>
                            <Grid Margin="5" Padding="0" HeightRequest="100" WidthRequest="220" Background="#80373737">
                                <Grid.RowDefinitions>

                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.7*"></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Image  Source="withoutbg.png"></Image>

                                <StackLayout Grid.Column="1" VerticalOptions="Center">
                                    <Label Grid.Column="1" Grid.Row="0" FontAttributes="Bold" TextColor="White" FontSize="25">21 999 ₽</Label>

                                    <Label Grid.Column="1" Grid.Row="1" TextColor="White" FontSize="15">Морозильная камера Haier HF-82WAA</Label>
                                </StackLayout>


                            </Grid>

                        </FlexLayout>
                    </ScrollView>

                </StackLayout>
        </Border>
    </Grid>
    </ScrollView>
</ContentPage>